<!--component/book/book.wxml-->
<view bind:tap="onTap" class='container' >
  <image src="{{book.image}}"></image>
  <text class="book-name">{{book.author}}</text>
  <text class="book-desc">{{book.title}}</text>
  <view><span></span><span></span></view>
</view>
